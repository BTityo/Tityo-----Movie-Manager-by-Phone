<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="PhoneClient.Views.ConnectionPage"
             Title="{Binding Title}"
             x:Name="BrowseItemsPage">
    <StackLayout>
        <Label Text="Server IP"/>
        <Editor Text="{Binding ServerIP, Mode=TwoWay}"/>
        <Label Text="Server Port"/>
        <Editor Text="{Binding Port, Mode=TwoWay}"/>
        <Button x:Name="buttonConnect" Text="Kapcsolódás..." Clicked="buttonConnect_Clicked"/>
        <Button x:Name="buttonConnectClose" Text="Kapcsolat bontása" Clicked="buttonConnectClose_Clicked" IsEnabled="False"/>
    </StackLayout>
</ContentPage>